import Provider from '@/store/Provider';

const PetListing = async () => {
  return (
    <div>
      <Provider>
        <h1> Home </h1>
      </Provider>
    </div>
  );
};

export default PetListing;
